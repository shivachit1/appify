import React from 'react';

import './App.css'

export const InstallPrompt = () => {

  return (
      <header id="prompt-header">
        <h1></h1>
        <p>Your PWA content here.</p>
        <button id="installButton">
          Install App
        </button>
      </header>
  );
};
